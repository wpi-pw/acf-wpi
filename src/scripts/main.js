window.$ = window.jQuery;

// import external dependencies
import objectFitImages from 'object-fit-images';

// On Load Events
$(window).on('load', () => {
	objectFitImages();
});

$(document).ready(() => {

});

